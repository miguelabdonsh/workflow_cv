apiVersion: v1
kind: Service
metadata:
  name: cv-analyzer-service
spec:
  selector:
    app: cv-analyzer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort  # Cambiado a NodePort para que minikube pueda exponerlo 